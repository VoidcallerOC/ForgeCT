{
  "name": "forge-ct",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Front Window marketing website",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint:html": "html-validate .",
    "test:links": "node scripts/check-local-links.mjs",
    "test:js": "node --check checkout.js && node --check api/checkout.js && node --check api/stripe-webhook.js && node --test api/stripe-webhook.test.js",
    "test": "npm run format:check && npm run lint:html && npm run test:links && npm run test:js"
  },
  "devDependencies": {
    "html-validate": "10.7.0",
    "prettier": "3.6.2"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "stripe": "^22.6.1"
  }
}
